[project]
name = "sf-bike-theft"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Drew Mathieson", email = "drewmathieson4@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
    "requests>=2.34.2",
]

[project.scripts]
sf-bike-theft = "sf_bike_theft:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
